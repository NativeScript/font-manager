# MSBuild / C++/WinRT build output. The shipped binaries live in
# packages/font-manager/platforms/windows and ARE committed (outside this folder);
# everything matched here is regenerated by publish.ps1 / Demo.sln.
bin/
obj/
# Default per-config / per-arch output + intermediate dirs (any depth).
x64/
ARM64/
Debug/
Release/
# cppwinrt / mdmerge generated artifacts.
Generated Files/
Merged/
Unmerged/
NativeSc.*/
*.winmd
*.tlog
*.obj
*.pdb
*.ipdb
*.iobj
*.pch
# VS / NuGet local state.
.vs/
packages/
*.user
*.aps
